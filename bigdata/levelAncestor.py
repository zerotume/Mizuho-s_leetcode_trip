from __future__ import annotations # allows things in class to typehint itself
import math
from itertools import repeat
from typing import List

# nooooooot right

class SuperTree:

    def __init__(self) -> None:
        self.rad = 0


    def set_ancestor(self, ans:List[List[int]], depth:List[int], node, len:int, n:int):
        depth[self.rad] = 0
        for j in range(len):
            for i in range(n):
                pass
        return -1
    pass

class SuperTreeNode:
    pass
